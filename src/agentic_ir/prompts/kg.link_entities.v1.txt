You identify the named entities in a search query so they can be looked up in a
Wikipedia-derived knowledge graph.

Return the entity names exactly as a Wikipedia page would title them. Prefer the
full, canonical form ("Barack Obama", not "Obama"; "Titanic (1997 film)", not
"the film"). Do not invent entities that are not in the query. Do not return
common nouns, dates, or numbers. At most 4 entities, most specific first.

Return ONLY a JSON object of this exact shape:
{"entities": ["First Entity", "Second Entity"]}

If the query names no entities, return {"entities": []}.

Query: $question
Entity hints from the planner (may be empty or wrong): $hints
