\chapter{System Implementation and Evaluation}
\label{ch:implementation}

\section{System Architecture}
% Map onto the brief's named modules: query planner, tool-invocation manager,
% retrieval engine, validation component. Orchestrator state machine.
% Cross-reference docs/architecture.md.

\section{Implementation Details}
\subsection{Indexing}
% BM25 index construction, FAISS index type, build time, memory footprint.
\subsection{Local Inference}
% Ollama, qwen3:8b, the 8GB VRAM budget and model placement. Structured JSON
% output and the deterministic fallback when parsing fails.
\subsection{Budget Enforcement}
% max_replans=2, max_llm_calls_per_question=20, max_wall_clock_s=300.
% Why an agentic system needs hard caps: without them a miscalibrated verifier
% turns a three-second query into an unbounded loop.

\section{Experimental Setup}
% Hardware (RTX 5060 Laptop 8GB, 31.6GB RAM), software versions, random seeds.
% Everything a third party needs to reproduce the numbers.

\section{Baselines}
% Ordered weakest to strongest so the agentic system must earn its result:
% bm25_only, dense_only, hybrid_rerank, naive_rag, self_ask.
% hybrid_rerank is the honest one to beat -- say so explicitly.
%
% MEASURED WARNING (n=20 smoke, hotpotqa, qwen3:8b) -- verify at n=250 before
% attributing anything to decomposition:
%   self_ask asked a follow-up on only 4 of 20 questions. Splitting EM by that:
%     - 4 follow-up questions : naive_rag .750 vs self_ask .750  (identical)
%     - 16 single-turn        : naive_rag .438 vs self_ask .625  (all the gain)
%   So self_ask's entire +0.15 EM came from questions where NO decomposition
%   occurred -- it is the decision turn ("can I answer yet?"), not the extra
%   hop. If this holds at n=250 the report must say so: it would mean the
%   multi-hop baseline's advantage is not multi-hop at all, which also changes
%   how much of agentic_full's margin can be credited to planning.

\section{Evaluation Metrics}
\subsection{Retrieval Quality}
% Recall@{2,5,10}, nDCG@10, MRR, Supporting-Fact EM/F1.
\subsection{Answer Quality}
% Exact Match, token-level F1.
\subsection{Agent-Specific Measures}
% LLM calls, tool calls, latency, plan depth, re-plan rate, citation grounding.
% Make the argument: a system that gains three F1 points for twenty times the
% compute has not obviously won. Reporting cost beside quality is what the
% brief means by agent-specific measures.

\section{Results}
\input{../results/tables/main_results.tex}
\input{../results/tables/agent_metrics.tex}
% Never hand-type numbers -- tables are generated so report and code cannot drift.

\section{Ablation Study}
\input{../results/tables/ablations.tex}
% Minus planner / minus kg / minus verifier. Attribute gains to components.
% If a component does not help, REPORT THAT. An honestly analysed negative
% ablation is worth more than a suppressed one.

\section{Error Analysis}
\input{../results/tables/error_analysis.tex}
% Sampled failures from the traces, categorised: decomposition errors, retrieval
% misses, entity-linking errors, verifier miscalibration.
% This is where the trace logging pays off.

\section{Discussion}
% Where agency helped, where it cost more than it returned, and under what
% conditions a practitioner should simply prefer the hybrid baseline.
