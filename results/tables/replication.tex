% Chapter 5 -- run-to-run drift on an identical configuration
% GENERATED by src/agentic_ir/eval/replication.py -- do not edit by hand.
% Regenerate with: python -m agentic_ir.eval.replication RUN_A RUN_B
% Source runs:
%   agentic_no_verifier/hotpotqa: agentic_no_verifier_hotpotqa_20260905T213927Z vs agentic_no_verifier_hotpotqa_20260906T145512Z (250 shared questions)

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{5pt}
  \caption{Run-to-run drift: the same configuration, the same questions, the same seed and a decoding temperature of 0.0, executed twice. \textit{Flipped} counts questions whose exact match changed between the two executions, and the column beside it how many of those also drew a different evidence pool -- locating the divergence in retrieval rather than in generation. The cause is the \texttt{PYTHONHASHSEED} column: Python randomises string hashing per process unless it is set, the evidence pool is assembled from sets, and set iteration order follows. \texttt{cli eval} sets it; \texttt{run\_eval} invoked directly inherits whatever the environment had, which for the evaluated grid was nothing. \textbf{This is the report's own error bar on execution}, and it is separate from every bootstrap interval elsewhere, all of which resample questions and hold the execution fixed. An effect smaller than this drift is not established by a single run.}
  \label{tab:replication-drift}
  \resizebox{\ifdim\width>\textwidth\textwidth\else\width\fi}{!}{%
  \begin{tabular}{llrrrrrrl}
    \toprule
    Configuration & Dataset & $n$ & EM run 1 & EM run 2 & $\Delta$EM & $\Delta$F1 & Flipped & \texttt{PYTHONHASHSEED} \\
    \midrule
    \texttt{agentic\_no\_verifier} & \texttt{hotpotqa} & 250 & 0.396 & 0.384 & -0.012 & -0.014 & 15 (13 via evidence) & \texttt{unset vs 0} \\
    \bottomrule
  \end{tabular}}
\end{table}
